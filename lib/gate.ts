/** Shared launch-gate constants (used by middleware + the unlock route). */
export const COOKIE = "sh_gate";
// The token stored in the cookie once unlocked (not the PIN itself).
export const GATE_TOKEN = "sh-open-6f2a";
